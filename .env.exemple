OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_MODEL=gpt-4.1-mini

# Optional: only set this for OpenAI-compatible endpoints.
# OPENAI_BASE_URL=https://api.openai.com/v1

# Optional: keep the harness open after the first prompt.
# KEEP_OPEN=1
